apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-events-exporter-config
  namespace: monitoring
data:
  config.yaml: |
    logLevel: info
    logFormat: json

    route:
      routes:
        - match:
            - receiver: "stdout"

    receivers:
      - name: "stdout"
        stdout: {}
