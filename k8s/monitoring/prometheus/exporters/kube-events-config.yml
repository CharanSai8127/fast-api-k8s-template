apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-events-exporter-config
  namespace: monitoring
data:
  config.yaml: |
    route:
      routes:
        - match:
            - receiver: "stdout"
    receivers:
      - name: "stdout"
