apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: gateway-appset
spec:
  goTemplate: true

  generators:
    - git:
        repoURL: https://github.com/CharanSai8127/fast-api-k8s-template.git
        revision: main
        directories:
          - path: k8s/overlays/prod/gateway

  template:
    metadata:
      name: gateway-prod
      annotations:
        argocd.argoproj.io/sync-wave: "-1"

    spec:
      project: default

      source:
        repoURL: https://github.com/CharanSai8127/fast-api-k8s-template.git
        targetRevision: main
        path: "{{.path.path}}"

      destination:
        server: https://kubernetes.default.svc
        namespace: nginx-gateway

      syncPolicy:
        automated:
          prune: true
          selfHeal: true
